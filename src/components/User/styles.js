import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexGrow: 1,
    marginBottom: 10,
    marginHorizontal: 10,
  },
  avatar: {
    width: 80,
    height: 80,
    borderRadius: 45,
  },
  name: {
    fontSize: 14,
    color: '#555',
  },
});
export default styles;
